# Claude Code local config — but keep launch.json (preview/dev-server config)
# tracked so any machine can run the same workflow after a plain clone.
.claude/*
!.claude/launch.json

# QA cache
qa/.cache/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Node
node_modules/
