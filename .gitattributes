*.pdf filter=lfs diff=lfs merge=lfs -text

# modules/registry.js is generated with LF and byte-compared by
# qa/registry_js_sync_lint.js — force LF on checkout so the QA gate
# passes on Windows clones (core.autocrlf=true would yield CRLF).
modules/registry.js text eol=lf
